// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://measure_user:vzl52b06gfEBxgGVCaCvUsRLo0raayEV@dpg-cr7h6vaj1k6c739s8hj0-a.oregon-postgres.render.com/measure"
}

enum MeasureType{
  WATER
  GAS
}

model Custumer{
  custumer_code       String @id @default(uuid())
  measures            Measure[]
}

model Measure{
  measure_uuid        String @id @default(uuid())
  measure_datetime    DateTime
  measure_type        MeasureType
  has_confirmed       Boolean @default(false)
  image_url           String

  custumer Custumer? @relation(fields: [custumer_code], references: [custumer_code])
  custumer_code      String
}
